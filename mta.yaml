_schema-version: "3.2"
ID: ppphoenixpaper
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: ppphoenixpaper-destination-content
  type: com.sap.application.content
  requires:
  - name: ppphoenixpaper-destination-service
    parameters:
      content-target: true
  - name: ppphoenixpaper-repo-host
    parameters:
      service-key:
        name: ppphoenixpaper-repo-host-key
  - name: ppphoenixpaper-uaa
    parameters:
      service-key:
        name: ppphoenixpaper-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: ppphoenixpaper_html_repo_host
          ServiceInstanceName: ppphoenixpaper-html5-service
          ServiceKeyName: ppphoenixpaper-repo-host-key
          sap.cloud.service: ppphoenixpaper
        - Authentication: OAuth2UserTokenExchange
          Name: ppphoenixpaper_uaa
          ServiceInstanceName: ppphoenixpaper-xsuaa-service
          ServiceKeyName: ppphoenixpaper-uaa-key
          sap.cloud.service: ppphoenixpaper
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: ppphoenixpaper-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ppphoenixpaper-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ppphoenixpaper.zip
      name: ppphoenixpaper
      target-path: resources/
- name: ppphoenixpaper
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ppphoenixpaper-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: ppphoenixpaper-destination-service
    service-plan: lite
- name: ppphoenixpaper-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: ppphoenixpaper-xsuaa-service
    service-plan: application
- name: ppphoenixpaper-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ppphoenixpaper-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
